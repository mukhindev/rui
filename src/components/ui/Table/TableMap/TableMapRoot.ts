import styled from 'styled-components';

export interface TableMapRootStyledOptions {}

const TableMapRoot = styled.div<TableMapRootStyledOptions>`
  position: relative;
`;

export default TableMapRoot;
